# Copy to .env. PAPER routes orders to spot.orderTest (validation only,
# never reaches the matching engine). LIVE routes to spot_newOrder.
# Anything other than an explicit MODE=LIVE is treated as PAPER.
MODE=PAPER
